# Core web framework
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic[email]>=2.6.0
pydantic-settings>=2.2.0
email-validator>=2.1.0

# Database
sqlalchemy>=2.0.0
alembic>=1.13.0

# Auth
bcrypt>=4.1.0
pyjwt>=2.8.0

# HTTP / async client
httpx>=0.27.0
requests>=2.31.0

# Template / path expressions
jmespath>=1.0.1

# Scheduling
APScheduler>=3.10.0

# File uploads / multipart
python-multipart>=0.0.9

# LLM clients (optional, only needed if LLM_PROVIDER=openai|anthropic)
openai>=1.30.0
anthropic>=0.28.0

# Email
aiosmtplib>=3.0.0

# YAML for workflow definition files
pyyaml>=6.0

# Test
pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx[http2]>=0.27.0

# Dev
ipython>=8.0.0
